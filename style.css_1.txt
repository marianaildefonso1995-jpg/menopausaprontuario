:root {
  --rosa: #d1548a;
  --rosa-clara: #f7e3ec;
  --rosa-escura: #a83c68;
  --fundo: #faf7f8;
  --texto: #2e2530;
  --texto-suave: #7a6c75;
  --borda: #e8dde3;
  --verde: #4c9a6a;
  --amarelo: #c98a1c;
  --vermelho: #c94c4c;
  --radius: 10px;
}

* { box-sizing: border-box; }

body {
  margin: 0;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
  background: var(--fundo);
  color: var(--texto);
}

.hidden { display: none !important; }

button {
  cursor: pointer;
  font-family: inherit;
  border: none;
  border-radius: var(--radius);
  padding: 10px 16px;
  font-size: 14px;
  font-weight: 600;
}

input, select, textarea {
  font-family: inherit;
  font-size: 14px;
  padding: 9px 12px;
  border: 1px solid var(--borda);
  border-radius: var(--radius);
  background: #fff;
  color: var(--texto);
  width: 100%;
}

label {
  font-size: 13px;
  font-weight: 600;
  color: var(--texto-suave);
  margin-top: 10px;
  display: block;
  margin-bottom: 4px;
}

.btn-primario {
  background: var(--rosa);
  color: #fff;
}
.btn-primario:hover { background: var(--rosa-escura); }

.btn-secundario {
  background: #fff;
  border: 1px solid var(--borda);
  color: var(--texto);
}
.btn-secundario:hover { background: var(--rosa-clara); }

.btn-mini {
  background: var(--rosa-clara);
  color: var(--rosa-escura);
  padding: 4px 10px;
  font-size: 12px;
}

.btn-perigo {
  background: #fff;
  border: 1px solid #f0c4c4;
  color: var(--vermelho);
}
.btn-perigo:hover { background: #fbe6e6; }

.ficha-header-acoes { display: flex; gap: 8px; }

.btn-fechar {
  background: none;
  font-size: 22px;
  color: var(--texto-suave);
  padding: 0 6px;
}

/* ===== LOGIN ===== */
.tela-login {
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, var(--rosa-clara), var(--fundo));
}
.card-login {
  background: #fff;
  padding: 40px;
  border-radius: 16px;
  box-shadow: 0 10px 40px rgba(0,0,0,0.08);
  width: 340px;
}
.card-login h1 { font-size: 20px; margin: 0 0 4px; color: var(--rosa-escura); }
.subtitulo { color: var(--texto-suave); margin: 0 0 20px; font-size: 14px; }
.card-login button { width: 100%; margin-top: 18px; }
.erro { color: var(--vermelho); font-size: 13px; margin-top: 8px; }

/* ===== APP SHELL ===== */
.app { display: flex; flex-direction: column; height: 100vh; }

.topo {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14px 24px;
  background: #fff;
  border-bottom: 1px solid var(--borda);
}
.topo-titulo { font-weight: 700; color: var(--rosa-escura); font-size: 17px; white-space: nowrap; }
.topo-usuario { display: flex; align-items: center; gap: 12px; font-size: 13px; color: var(--texto-suave); }

/* busca de aluna sempre visível no topo, funciona de qualquer seção */
.topo-busca { position: relative; flex: 1; max-width: 420px; margin: 0 24px; }
.topo-busca input { width: 100%; margin: 0; }
.busca-resultado {
  position: absolute;
  top: calc(100% + 6px);
  left: 0; right: 0;
  background: #fff;
  border: 1px solid var(--borda);
  border-radius: var(--radius);
  box-shadow: 0 8px 24px rgba(0,0,0,.12);
  max-height: 340px;
  overflow-y: auto;
  z-index: 60;
  padding: 6px;
}
.busca-resultado .item-aluna { margin-bottom: 4px; }
.busca-resultado .item-aluna:last-child { margin-bottom: 0; }

.corpo { flex: 1; display: flex; overflow: hidden; }

/* ===== SIDEBAR ===== */
.sidebar {
  width: 300px;
  background: #fff;
  border-right: 1px solid var(--borda);
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  overflow-y: auto;
}
.sidebar-acoes { display: flex; gap: 8px; }
.sidebar-acoes button { flex: 1; font-size: 13px; padding: 9px; }

.ajuda-campo { font-size: 12px; color: var(--texto-suave); margin: 4px 0 0; }

#busca-global { margin-bottom: 4px; }

.btn-link {
  background: none;
  color: var(--rosa-escura);
  padding: 4px 0;
  font-size: 13px;
  text-align: left;
}
.btn-link:hover { text-decoration: underline; }

.lista-turmas { display: flex; flex-direction: column; gap: 8px; margin-top: 6px; }
.item-turma {
  padding: 12px;
  border: 1px solid var(--borda);
  border-radius: var(--radius);
  cursor: pointer;
}
.item-turma:hover { background: var(--rosa-clara); }
.item-turma-nome { font-weight: 700; font-size: 14px; color: var(--rosa-escura); }
.item-turma-periodo { font-size: 12px; color: var(--texto-suave); margin-top: 2px; }

/* na sidebar estreita o nome da turma e os botões não cabem lado a lado — empilha */
.turma-aberta-header { display: flex; flex-direction: column; align-items: flex-start; gap: 8px; margin-top: 4px; }
.turma-aberta-header h3 { margin: 0; font-size: 16px; }
.turma-aberta-header .acoes-linha { flex-wrap: wrap; }
.turma-aberta-periodo { font-size: 12px; color: var(--texto-suave); margin: 4px 0 12px; }

.lista-alunas { display: flex; flex-direction: column; gap: 8px; margin-top: 6px; }

.item-aluna {
  padding: 10px 12px;
  border: 1px solid var(--borda);
  border-radius: var(--radius);
  cursor: pointer;
}
.item-aluna:hover { background: var(--rosa-clara); }
.item-aluna.ativo { background: var(--rosa-clara); border-color: var(--rosa); }
.item-aluna-nome { font-weight: 600; font-size: 14px; }
.item-aluna-meta { font-size: 12px; color: var(--texto-suave); margin-top: 2px; }
.item-aluna-badges { display: flex; gap: 6px; margin-top: 6px; flex-wrap: wrap; }

.vazio { color: var(--texto-suave); font-size: 13px; font-style: italic; }

/* ===== BADGES ===== */
.badge {
  display: inline-block;
  font-size: 11px;
  font-weight: 700;
  padding: 3px 8px;
  border-radius: 20px;
  background: var(--rosa-clara);
  color: var(--rosa-escura);
}
.badge.manutencao { background: #e3eef7; color: #2f6a9e; }
.badge.atrasado { background: #fbe6e6; color: var(--vermelho); }
.badge.pesagem-hoje { background: #fff3d6; color: var(--amarelo); }
.badge.pesagem-atrasada { background: #fbe6e6; color: var(--vermelho); }
.badge.status-ativa { background: #e5f4ea; color: var(--verde); }
.badge.status-pausada { background: #fff3d6; color: var(--amarelo); }
.badge.status-concluida { background: #e8dde3; color: var(--texto-suave); }

/* ===== CONTEÚDO / FICHA ===== */
.conteudo { flex: 1; padding: 24px; overflow-y: auto; }

.ficha-vazia {
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--texto-suave);
}

/* caminho de navegação: Turmas › Turma X › Aluna Y */
.caminho { display: flex; align-items: center; gap: 6px; flex-wrap: wrap; margin-bottom: 10px; font-size: 12px; }
.caminho-link {
  background: none; border: none; padding: 0;
  font-family: inherit; font-size: 12px;
  color: var(--rosa-escura); cursor: pointer; text-decoration: underline;
}
.caminho-link:hover { color: var(--rosa); }
.caminho-sep { color: var(--texto-suave); }
.caminho-atual { color: var(--texto-suave); font-weight: 600; }

/* resumo de progresso no topo da ficha */
.ficha-resumo {
  background: var(--rosa-clara);
  border: 1px solid var(--borda);
  border-radius: var(--radius);
  padding: 14px 16px;
  margin-bottom: 16px;
}
.resumo-vazio { margin: 0; font-size: 13px; color: var(--texto-suave); }
.resumo-numeros { display: grid; grid-template-columns: repeat(auto-fit, minmax(110px, 1fr)); gap: 12px; }
.resumo-item { display: flex; flex-direction: column; gap: 2px; }
.resumo-item-largo { grid-column: span 2; min-width: 180px; }
.resumo-label { font-size: 11px; color: var(--texto-suave); text-transform: uppercase; letter-spacing: .3px; }
.resumo-valor { font-size: 17px; font-weight: 700; color: var(--rosa-escura); }
.resumo-valor.bom { color: var(--verde); }
.resumo-valor.ruim { color: var(--vermelho); }
.resumo-valor.aviso { color: var(--amarelo); }
.resumo-item-largo .resumo-valor { font-size: 14px; }

.barra-progresso {
  position: relative;
  margin-top: 14px;
  height: 22px;
  background: #fff;
  border: 1px solid var(--borda);
  border-radius: 999px;
  overflow: hidden;
}
.barra-progresso-preenchida { height: 100%; background: linear-gradient(90deg, var(--rosa), var(--rosa-escura)); transition: width .4s; }
.barra-progresso-texto {
  position: absolute; inset: 0;
  display: flex; align-items: center; justify-content: center;
  font-size: 11px; font-weight: 700; color: var(--texto);
  /* o texto fica em cima da parte preenchida e da vazia, então o contorno branco garante leitura */
  text-shadow: 0 0 3px #fff, 0 0 5px #fff, 0 0 7px #fff;
}

/* gráfico de evolução do peso */
.grafico-peso { margin: 18px 0; }
.grafico-titulo { margin: 0 0 8px; font-size: 14px; color: var(--rosa-escura); }
.grafico-vazio, .grafico-legenda { font-size: 12px; color: var(--texto-suave); margin: 6px 0 0; }
.g-svg { width: 100%; height: auto; background: #fff; border: 1px solid var(--borda); border-radius: var(--radius); }
.g-grade { stroke: var(--borda); stroke-width: 1; }
.g-eixo { font-size: 10px; fill: var(--texto-suave); }
.g-area { fill: var(--rosa-clara); opacity: .7; }
.g-linha { fill: none; stroke: var(--rosa-escura); stroke-width: 2.5; stroke-linejoin: round; stroke-linecap: round; }
.g-ponto { fill: #fff; stroke: var(--rosa-escura); stroke-width: 2.5; cursor: pointer; }
.g-ponto:hover { fill: var(--rosa); r: 6; }
.g-meta { stroke: var(--verde); stroke-width: 2; stroke-dasharray: 6 4; }
.g-meta-texto { font-size: 10px; fill: var(--verde); font-weight: 700; }

/* pendências já resolvidas */
.bloco-resolvidas { margin-top: 20px; }
.lembrete-card.resolvida { opacity: .75; border-left-color: var(--verde); }
#lista-resolvidas { margin-top: 10px; }

.ficha-header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 12px; }
.ficha-header h2 { margin: 0 0 4px; }
.ficha-subheader { font-size: 13px; color: var(--texto-suave); }

.alerta {
  padding: 12px 14px;
  border-radius: var(--radius);
  margin-bottom: 14px;
  font-size: 14px;
}
.alerta.info { background: #fff3d6; color: #8a6008; }
.alerta.atencao { background: #fbe6e6; color: #9a2f2f; }

.abas { display: flex; gap: 4px; border-bottom: 1px solid var(--borda); margin-bottom: 18px; }
.aba-botao {
  background: none;
  color: var(--texto-suave);
  border-radius: 0;
  padding: 10px 16px;
  border-bottom: 2px solid transparent;
}
.aba-botao.ativo { color: var(--rosa-escura); border-bottom-color: var(--rosa); }

.titulo-secao { font-size: 15px; color: var(--rosa-escura); margin: 0 0 10px; }
.titulo-secao-espaco { margin-top: 28px; padding-top: 20px; border-top: 1px solid var(--borda); }

.checklist-exames {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
  gap: 8px 16px;
  margin-bottom: 12px;
}
.checklist-item {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 14px;
  font-weight: 400;
  color: var(--texto);
  margin: 0;
  cursor: pointer;
}
.checklist-item input { width: auto; }

#form-pedido-exame { margin-bottom: 16px; }
#form-pedido-exame input[type="text"], #form-pedido-exame input[type="date"] { margin-bottom: 8px; }

.grid-info {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
  gap: 12px;
  margin-bottom: 20px;
}
.info-box {
  background: #fff;
  border: 1px solid var(--borda);
  border-radius: var(--radius);
  padding: 12px;
}
.info-label { display: block; font-size: 11px; text-transform: uppercase; color: var(--texto-suave); font-weight: 700; margin-bottom: 4px; }
.info-valor { font-size: 15px; font-weight: 600; }
.info-valor.atrasado { color: var(--vermelho); }

.bloco-texto {
  background: #fff;
  border: 1px solid var(--borda);
  border-radius: var(--radius);
  padding: 14px 16px;
  margin-bottom: 14px;
}
.bloco-texto h3 { margin: 0 0 8px; font-size: 14px; color: var(--rosa-escura); }
.bloco-texto p { margin: 0; font-size: 14px; line-height: 1.5; white-space: pre-wrap; }
.bloco-texto-header { display: flex; justify-content: space-between; align-items: center; }

.form-inline { display: flex; gap: 8px; margin-bottom: 16px; flex-wrap: wrap; }
.form-inline input { flex: 1; min-width: 120px; }

.form-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
  gap: 8px;
  margin-bottom: 16px;
  align-items: center;
}
.form-grid button { grid-column: 1 / -1; }

.lista-pesos, .lista-exames, .lista-entregas { display: flex; flex-direction: column; gap: 8px; }

.linha-peso, .linha-entrega {
  display: flex;
  gap: 14px;
  align-items: center;
  background: #fff;
  border: 1px solid var(--borda);
  border-radius: var(--radius);
  padding: 10px 14px;
  font-size: 14px;
}
.linha-peso .obs, .linha-entrega .obs { color: var(--texto-suave); font-size: 13px; }

.linha-exame {
  background: #fff;
  border: 1px solid var(--borda);
  border-radius: var(--radius);
  padding: 12px 14px;
}
.linha-exame-meta { font-size: 12px; color: var(--texto-suave); margin-top: 4px; }
.tag-tipo, .tag-status {
  font-size: 11px;
  font-weight: 700;
  padding: 2px 8px;
  border-radius: 20px;
  margin-left: 8px;
}
.tag-tipo { background: var(--rosa-clara); color: var(--rosa-escura); }
.tag-status.status-pendente { background: #fff3d6; color: var(--amarelo); }
.tag-status.status-concluido { background: #e5f4ea; color: var(--verde); }
.tag-status.status-enviado { background: #e5f4ea; color: var(--verde); }
.link-anexo { display: inline-block; margin-top: 6px; font-size: 13px; color: var(--rosa-escura); }

.cota { font-size: 13px; font-weight: 700; color: var(--texto-suave); margin-bottom: 10px; }
.cota.estourada { color: var(--vermelho); }

/* ===== LEVAS DE PEDIDO DE EXAME ===== */
.lista-pedidos { display: flex; flex-direction: column; gap: 10px; margin-bottom: 8px; }

.leva-pedido {
  background: #fff;
  border: 1px solid var(--borda);
  border-radius: var(--radius);
  overflow: hidden;
}
.leva-pedido-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 14px;
  cursor: pointer;
}
.leva-pedido-header:hover { background: var(--rosa-clara); }
.leva-pedido-titulo { font-weight: 700; font-size: 14px; }
.leva-pedido-sub { font-size: 12px; color: var(--texto-suave); font-weight: 400; margin-left: 8px; }
.leva-pedido-acoes { display: flex; align-items: center; gap: 8px; }

.leva-pedido-itens {
  border-top: 1px solid var(--borda);
  padding: 10px 14px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.leva-pedido-itens.hidden { display: none; }

.item-exame {
  display: grid;
  grid-template-columns: 1fr 1.4fr 140px auto;
  gap: 8px;
  align-items: center;
}
.item-exame-nome { font-size: 14px; font-weight: 600; }
.item-exame input { font-size: 13px; padding: 7px 10px; }

@media (max-width: 700px) {
  .item-exame { grid-template-columns: 1fr; }
}

/* ===== NAV PRINCIPAL (Turmas / Calendário / Clubinho) ===== */
.nav-principal { display: flex; gap: 4px; margin-bottom: 4px; }
.nav-botao {
  flex: 1;
  background: none;
  color: var(--texto-suave);
  font-size: 12px;
  padding: 8px 4px;
  border-radius: var(--radius);
  border: 1px solid var(--borda);
}
.nav-botao:hover { background: var(--rosa-clara); }
.nav-botao.ativo { background: var(--rosa); color: #fff; border-color: var(--rosa); }

/* ===== VISÃO GERAL ===== */
.vg-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 18px; flex-wrap: wrap; gap: 10px; }
.vg-importar { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
.vg-importar select { width: auto; min-width: 200px; }
.vg-titulo { margin: 0; font-size: 20px; color: var(--rosa-escura); }

.vg-resumo { display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 12px; margin-bottom: 8px; }
.vg-card {
  background: #fff;
  border: 1px solid var(--borda);
  border-radius: var(--radius);
  padding: 16px;
  text-align: center;
  cursor: pointer;
  font-family: inherit;
  transition: border-color .15s, background .15s, box-shadow .15s;
}
.vg-card:hover { border-color: var(--rosa); background: var(--rosa-clara); }
.vg-card.ativo {
  border-color: var(--rosa);
  background: var(--rosa-clara);
  box-shadow: inset 0 -3px 0 var(--rosa);
}
.vg-card-numero { display: block; font-size: 28px; font-weight: 700; color: var(--rosa-escura); }
.vg-card-label { display: block; font-size: 12px; color: var(--texto-suave); }
.vg-card-dica { display: block; font-size: 11px; color: var(--rosa-escura); margin-top: 6px; opacity: .75; }
.vg-card.ativo .vg-card-dica { opacity: 0; }

.vg-painel { margin-top: 4px; }
/* cabeçalho do painel: título de um lado, filtros/busca do outro, alinhados na mesma linha */
.vg-painel .bloco-texto-header { align-items: flex-end; gap: 12px; margin-top: 28px; padding-top: 20px; border-top: 1px solid var(--borda); flex-wrap: wrap; }
.vg-painel .bloco-texto-header .titulo-secao { margin: 0; padding-top: 0; border-top: none; }
.vg-filtros { display: flex; gap: 6px; flex-wrap: wrap; }
.chip-filtro {
  background: #fff;
  border: 1px solid var(--borda);
  border-radius: 999px;
  padding: 5px 12px;
  font-size: 12px;
  cursor: pointer;
  font-family: inherit;
  color: var(--texto-suave);
}
.chip-filtro:hover { border-color: var(--rosa); color: var(--rosa-escura); }
.chip-filtro.ativo { background: var(--rosa); border-color: var(--rosa); color: #fff; font-weight: 600; }

.vg-busca { width: auto; min-width: 220px; margin: 0; }

.vg-lista-cards { display: flex; flex-direction: column; gap: 10px; }
.vg-card-item {
  background: #fff;
  border: 1px solid var(--borda);
  border-radius: var(--radius);
  padding: 12px 16px;
  cursor: pointer;
  transition: border-color .15s, background .15s;
}
.vg-card-item:hover { border-color: var(--rosa); background: var(--rosa-clara); }
.vg-card-item-titulo { font-weight: 700; font-size: 14px; }
.vg-card-item-meta { font-size: 12px; color: var(--texto-suave); margin-top: 2px; }

.badge.tudo-ok { background: #e5f4ea; color: var(--verde); }
.btn-resolvido { background: #e5f4ea; color: var(--verde); border-color: #cbe6d6; }

.lista-lembretes { display: flex; flex-direction: column; gap: 10px; }
.lembrete-card {
  background: #fff;
  border: 1px solid var(--borda);
  border-left: 4px solid var(--borda);
  border-radius: var(--radius);
  padding: 12px 16px;
}
.lembrete-card.cor-verde { border-left-color: var(--verde); }
.lembrete-card.cor-amarelo { border-left-color: var(--amarelo); }
.lembrete-card.cor-vermelho { border-left-color: var(--vermelho); }
.lembrete-titulo { font-weight: 700; font-size: 14px; }
.lembrete-texto { font-size: 13px; color: var(--texto-suave); margin-top: 2px; }
.lembrete-tipo {
  display: inline-block;
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  color: var(--texto-suave);
  background: var(--rosa-clara);
  border-radius: 20px;
  padding: 2px 8px;
  margin-bottom: 4px;
}
.lembrete-acoes { display: flex; gap: 8px; margin-top: 10px; flex-wrap: wrap; }
.lembrete-acoes select { width: auto; flex: 1; min-width: 160px; }
.lembrete-acoes .btn-mini, .lembrete-acoes .btn-link { padding: 7px 12px; font-size: 12px; }

/* ===== PAINEL DIREITO (calendário, sempre visível, canto oposto às turmas) ===== */
.painel-direita {
  width: 280px;
  flex-shrink: 0;
  background: #fff;
  border-left: 1px solid var(--borda);
  padding: 16px;
  overflow-y: auto;
}

/* ===== CALENDÁRIO GERAL ===== */
.btn-bloco { display: block; width: 100%; margin-bottom: 14px; }

.calendario-header { display: flex; align-items: center; gap: 10px; margin-bottom: 12px; }
.calendario-header h2 { flex: 1; text-align: center; margin: 0; font-size: 16px; }

.grade-calendario {
  display: grid !important;
  grid-template-columns: repeat(7, 1fr);
  gap: 4px;
  margin-bottom: 8px;
}
.dia-semana { text-align: center; font-size: 10px; font-weight: 700; color: var(--texto-suave); padding: 2px 0; }
.dia-calendario {
  min-height: 40px;
  border: 1px solid var(--borda);
  border-radius: 8px;
  padding: 4px;
  background: #fff;
  font-size: 11px;
  cursor: pointer;
}
.dia-calendario:hover { background: var(--rosa-clara); }
.dia-calendario.fora-do-mes { visibility: hidden; cursor: default; }
.dia-calendario.hoje { border-color: var(--rosa); border-width: 2px; }
.dia-numero { font-weight: 700; }
.dia-bolinhas { display: flex; gap: 2px; flex-wrap: wrap; margin-top: 3px; }

.bolinha { width: 7px; height: 7px; border-radius: 50%; display: inline-block; }
.bolinha.verde { background: var(--verde); }
.bolinha.amarelo { background: var(--amarelo); }
.bolinha.vermelho { background: var(--vermelho); }
.bolinha.passado { background: var(--borda); }
.item-clubinho-badge { width: 9px; height: 9px; }

.lista-eventos { display: flex; flex-direction: column; gap: 8px; }
.linha-evento {
  display: flex;
  flex-direction: column;
  gap: 6px;
  background: #fff;
  border: 1px solid var(--borda);
  border-left: 4px solid var(--borda);
  border-radius: var(--radius);
  padding: 10px 14px;
  font-size: 14px;
}
.linha-evento.cor-verde { border-left-color: var(--verde); }
.linha-evento.cor-amarelo { border-left-color: var(--amarelo); }
.linha-evento.cor-vermelho { border-left-color: var(--vermelho); }
.linha-evento.cor-passado { border-left-color: var(--borda); }
.linha-evento-info { min-width: 0; }
.linha-evento-titulo { font-weight: 600; word-break: break-word; }
.linha-evento-meta { font-size: 12px; color: var(--texto-suave); margin-top: 2px; }

.linha-evento-rodape { display: flex; align-items: center; justify-content: space-between; gap: 8px; }

.linha-evento-contagem {
  font-size: 11px;
  font-weight: 700;
  padding: 3px 9px;
  border-radius: 20px;
  color: #fff;
  white-space: nowrap;
}
.linha-evento-contagem.cor-verde { background: var(--verde); }
.linha-evento-contagem.cor-amarelo { background: var(--amarelo); }
.linha-evento-contagem.cor-vermelho { background: var(--vermelho); }
.linha-evento-contagem.cor-passado { background: var(--texto-suave); }

@media (max-width: 900px) {
  .painel-direita { width: 100%; border-left: none; border-top: 1px solid var(--borda); }
}

/* ===== BOTÕES DE AÇÃO PEQUENOS (editar/excluir em listas) ===== */
.acoes-linha { display: flex; gap: 4px; margin-left: auto; }
.btn-icone {
  background: none;
  padding: 4px 6px;
  font-size: 14px;
  color: var(--texto-suave);
  border-radius: 6px;
}
.btn-icone:hover { background: var(--rosa-clara); }
.btn-icone.excluir:hover { background: #fbe6e6; color: var(--vermelho); }

/* ===== MODAL ===== */
.modal {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.4);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
  z-index: 50;
}
.modal-conteudo {
  background: #fff;
  border-radius: 14px;
  padding: 24px;
  width: 100%;
  max-width: 560px;
  max-height: 90vh;
  overflow-y: auto;
}
.modal-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 6px; }
.modal-header h2 { margin: 0; font-size: 18px; }
.modal-acoes { display: flex; justify-content: flex-end; gap: 10px; margin-top: 20px; }
.form-2col { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }

/* ===== TOAST ===== */
.toast {
  position: fixed;
  bottom: 24px;
  left: 50%;
  transform: translateX(-50%) translateY(20px);
  background: var(--texto);
  color: #fff;
  padding: 12px 20px;
  border-radius: 30px;
  font-size: 14px;
  opacity: 0;
  pointer-events: none;
  transition: all 0.25s ease;
  z-index: 100;
}
.toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }
.toast.erro { background: var(--vermelho); }

/* ===== CELULAR E TABLET =====
   No desktop cada coluna rola sozinha (corpo com overflow hidden). No celular
   isso espremia tudo em faixinhas; aqui a página inteira passa a rolar normal,
   uma seção embaixo da outra: menu → conteúdo → calendário. */
@media (max-width: 800px) {
  html, body { overflow-x: hidden; }
  .app { height: auto; min-height: 100vh; }

  .topo { flex-wrap: wrap; gap: 10px; padding: 12px 16px; }
  .topo-titulo { font-size: 15px; }
  .topo-busca { order: 3; flex: 1 1 100%; max-width: none; margin: 0; }
  .topo-usuario { font-size: 12px; gap: 8px; }

  .corpo { flex-direction: column; overflow: visible; }
  .sidebar {
    width: 100%;
    border-right: none;
    border-bottom: 1px solid var(--borda);
    max-height: none;
    overflow-y: visible;
    padding: 14px 16px;
  }
  .conteudo { padding: 16px; overflow-y: visible; }
  .painel-direita {
    width: 100%;
    border-left: none;
    border-top: 1px solid var(--borda);
    overflow-y: visible;
    padding: 14px 16px;
  }

  .form-2col { grid-template-columns: 1fr; }
  .form-inline { flex-direction: column; align-items: stretch; }
  .form-inline > * { width: 100%; }

  /* cards e grades: menos colunas pra não espremer o texto */
  .vg-resumo { grid-template-columns: 1fr 1fr; }
  .vg-resumo .vg-card:first-child { grid-column: span 2; }
  .grid-info { grid-template-columns: 1fr 1fr; }
  .resumo-numeros { grid-template-columns: 1fr 1fr; }
  .resumo-item-largo { grid-column: span 2; }

  /* cabeçalhos que no desktop ficam lado a lado passam a empilhar */
  .ficha-header, .bloco-texto-header, .vg-painel .bloco-texto-header, .vg-header {
    flex-direction: column;
    align-items: stretch;
    gap: 10px;
  }
  .ficha-header-acoes { flex-wrap: wrap; }
  .vg-busca { width: 100%; min-width: 0; }

  /* abas da ficha viram rolagem horizontal em vez de quebrar linha */
  .abas { overflow-x: auto; flex-wrap: nowrap; -webkit-overflow-scrolling: touch; }
  .abas .aba-botao { white-space: nowrap; flex-shrink: 0; }

  /* calendário e listas: números menores pra caber em 7 colunas */
  .grade-calendario { gap: 2px; }
  .lembrete-acoes { flex-wrap: wrap; }
  .linha-peso { flex-wrap: wrap; gap: 6px; }
  .modal-conteudo { padding: 18px; }
}

/* telas bem estreitas (celular pequeno) */
@media (max-width: 420px) {
  .vg-resumo, .grid-info { grid-template-columns: 1fr; }
  .vg-resumo .vg-card:first-child { grid-column: span 1; }
  /* o resumo continua em 2 colunas: são números curtos e caberia mal empilhado */
  .resumo-numeros { grid-template-columns: 1fr 1fr; }
  .topo-titulo { font-size: 14px; }
}
